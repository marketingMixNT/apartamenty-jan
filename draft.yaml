models:
  PageAbout:
    meta_title: json
    meta_desc: json
    header_heading: json
    heading: json
    relationships:
      hasMany:
        PageAboutBlock

  PageAboutBlock:
    image: text
    text: json
    relationships:
      belongsTo:
        PageAbout
